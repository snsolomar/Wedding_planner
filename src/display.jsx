import ListView from "./listView";

const Display = (props) => {
    return (
    <div className="Display" data-testid="navDisplay">
        <ListView />
    </div>
    );
}
  
export default Display;